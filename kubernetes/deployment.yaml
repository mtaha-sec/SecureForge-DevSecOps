apiVersion: apps/v1
kind: Deployment

metadata:
  name: juice-shop
  namespace: devsecops

spec:
  replicas: 2

  selector:
    matchLabels:
      app: juice-shop

  template:
    metadata:
      labels:
        app: juice-shop

    spec:
      containers:

        - name: juice-shop

          image: juice-shop:latest

          imagePullPolicy: Never

          ports:
            - containerPort: 3000

          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"

            limits:
              memory: "512Mi"
              cpu: "500m"
